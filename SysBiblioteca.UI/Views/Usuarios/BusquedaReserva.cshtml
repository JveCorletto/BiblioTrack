@{
    ViewData["Title"] = "BusquedaReserva";
    Layout = "~/Views/Shared/_emptyLayout.cshtml";
}

<!--Subheader-->
<div class="subheader py-6 py-lg-8 subheader-transparent" id="kt_subheader">
    <div class="container d-flex align-items-center justify-content-between flex-wrap flex-sm-nowrap">
        <div class="d-flex align-items-center flex-wrap mr-1">
            <div class="d-flex align-items-baseline flex-wrap mr-5">
                <h2 class="text-dark font-weight-bold my-1 mr-5"><i class="fas fa-book"></i> Búsqueda y reserva de libros</h2>
            </div>
        </div>
    </div>
</div>

<!--Cuerpo del Docto-->
<div class="d-flex flex-column-fluid">
    <div class="container">
        <div class="card card-custom">
            <div class="card-body">
                <table id="tableReserva" class="table table-hover">
                    <thead class="thead-dark">
                        <tr>
                            <th scope="col">Libro</th>
                            <th scope="col">Autor</th>
                            <th scope="col">Información</th>
                            <th scope="col">Número RFID</th>
                            <th scope="col">Estado</th>
                            <th scope="col" class='text-center' width="150">Acciones</th>
                        </tr>
                    </thead>
                    <tbody id="tActivos"></tbody>
                </table>
            </div>
        </div>
    </div>
</div>
